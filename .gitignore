# == Rails ==
# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile '~/.gitignore_global'

# Ignore bundler config.
postapi/.bundle

# Ignore the default SQLite database.
postapi/db/*.sqlite3
postapi/db/*.sqlite3-*

# Ignore all logfiles and tempfiles.
postapi/log/*
postapi/tmp/*
!postapi/log/.keep
!postapi/tmp/.keep

# Ignore pidfiles, but keep the directory.
postapi/tmp/pids/*
!postapi/tmp/pids/
!postapi/tmp/pids/.keep

# Ignore uploaded files in development.
postapi/storage/*
!postapi/storage/.keep
postapi/tmp/storage/*
!postapi/tmp/storage/
!postapi/tmp/storage/.keep

# Ignore master key for decrypting credentials and more.
postapi/config/master.key



# == Flutter ==
# Miscellaneous
postclient/*.class
postclient/*.log
postclient/*.pyc
postclient/*.swp
postclient/.DS_Store
postclient/.atom/
postclient/.buildlog/
postclient/.history
postclient/.svn/
postclient/migrate_working_dir/

# IntelliJ related
postclient/*.iml
postclient/*.ipr
postclient/*.iws
postclient/.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
postclient/**/doc/api/
postclient/**/ios/Flutter/.last_build_id
postclient/.dart_tool/
postclient/.flutter-plugins
postclient/.flutter-plugins-dependencies
postclient/.packages
postclient/.pub-cache/
postclient/.pub/
postclient/build/

# Web related
postclient/lib/generated_plugin_registrant.dart

# Symbolication related
postclient/app.*.symbols

# Obfuscation related
postclient/app.*.map.json

# Android Studio will place build artifacts here
postclient/android/app/debug
postclient/android/app/profile
postclient/android/app/release
